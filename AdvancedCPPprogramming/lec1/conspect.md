## Лекция 1 (0)

### Краткий опрос

```cpp
class A{};

A f(){
    return A();
}

A a = f(); // выполняется компилятором как A a();
```

```cpp 
class A{
    int x;
    int y;
    void* pim;
};
```
важно когда объект владеет каким-то ресурсом, при перемещении (наивной операции копирования)

**std::move** - приводит объект к rvalue

**delete** - удаление указателя или ключевое слово при отключении явного конструктора

компилятор за нас формирует код конструктора (дефолтный)

A() = delete или A() = default 

**vector, list, map** (основан на красно-черном дереве)

у **vector** добавление за константу или может быть за другую константу

**итераторы, инвалидация итераторов**

**constexpr** - значение может быть вычисленно на этапе компиляции (к переменной, к функции, )

**constepr if(...)** - в момент компиляции может быть полностью вычислено (пусть False => тогда блок выкидывается из кода - некоторая оптимизация)

```cpp
constexpr if(5<0){
    ... //неважно что мы тут напишем, мы его выкинем уже на этапе комипляции
}
```
Если нельзя определить constexpr - compilation error

Отмена коммита - **git reset** (soft - изменяет, hard - удаляет навсегда)

**static**: отключает видимость извне

file1.cpp file2.cpp

void f1() static void f2()

extern void f2()

чтобы я мог вызвать функцию необходим #include

**noexcept** - ? 

**override** - переопределение виртуальной функции  (требует у переопределяемой функции чтобы была виртуальность)

**enum** - перечисление

```cpp
enum /*class*/ NPCClass{
    Warior; //0
    Mag;    //1, можно переопределить Mag = 10
    Rogue;  //2
};

int a = Warior;

NPCClass b = Mag;

NPCClass c = NPCClass::Rogue;
```
см файл 1.cpp

**protected** - private вне классов, но может вызываться у наследников (public)

**malloc free** - выделение памяти при освобождении

**new delete** - сами по себе вызывают внутри malloc, delete - free, new вызывает конструктор, delete - вызывает деструктор

**#include** - подставляет содержимое файла в код

в 23 стандарте появились модули (но не полностью реализовано пока что)

**#define** определяется на этапе препроцессинга

**one defenition rule** - каждая функция должна определяться один раз, 2 раза нельзя (на этапе линковки будет ошибка)

**Шаблоны** - механизм, позволяющий универсализировать функции под некоторые типы данных (классы в тч)

Через виртуальные функции полиморфизм у нас динамический, а шаблоны создают динамический

**Правило 0 3 5** - ?