## Лекция 2 (0.5)

Поговорим про 
1) системы автоматизированной сборки
2) системы контроля версий

Cmake, msbuild, qtbuild и тд

-Wall, -Werrors - рекумендуется компилировать с этим

```cpp
int a;

void f(int& a);
f(a); // здесь ворнинг
void f(int a, int b) {
    int a;
    //не используемый аргумент ф-ии, также может быть неисполльзуемый результат функции
}
```
Rebase, Merge, Cherry-peak

